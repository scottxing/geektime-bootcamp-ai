---
import PageLayout from "../../layouts/PageLayout.astro";
import PageHeader from "../../components/ui/PageHeader";
import ProjectCard from "../../components/projects/ProjectCard";
import ScrollReveal from "../../components/ui/ScrollReveal";
import { projects } from "../../data/projects";
import { getUrl } from "../../utils/url";
---

<PageLayout
  title="实战项目 - 陈天 AI 训练营"
  description="通过 5 个实战项目，从零到一掌握 AI 辅助编程"
>
  <PageHeader
    title="实战项目"
    subtitle="从零到一，用 AI 构建真实项目"
    client:load
  >
    <div class="justify-center md-cta-stack">
      <a href="#projects" class="md-btn"> 查看项目 </a>
      <a href={getUrl("curriculum")} class="md-btn md-btn-secondary">
        返回课程
      </a>
    </div>
  </PageHeader>

  <section class="md-section">
    <div class="md-container">
      <ScrollReveal client:load>
        <h2 class="font-bold text-center text-h2 text-ink mb-space-5">
          循序渐进的项目设计
        </h2>
        <p
          class="mx-auto max-w-2xl text-center mb-space-9 text-body text-slate"
        >
          从简单到复杂，每个项目都对应特定的学习目标，逐步提升你的 AI
          辅助编程能力
        </p>
      </ScrollReveal>

      <div class="grid grid-cols-1 gap-space-2">
        {
          projects.map((project, index) => (
            <ScrollReveal delay={index * 0.1} client:load>
              <div class="flex flex-col items-start md:flex-row gap-space-8">
                <div class="md:w-2/3">
                  <ProjectCard
                    number={project.number}
                    title={project.title}
                    subtitle={project.subtitle}
                    difficulty={project.difficulty}
                    estimatedHours={project.estimatedHours}
                    techStack={project.techStack}
                    href={getUrl(`projects/project-${project.number}`)}
                    previewImage={project.previewImage}
                    client:load
                  />
                </div>
                <div class="md:w-1/3 space-y-space-4">
                  <div class="md-card p-space-6">
                    <h4 class="font-semibold text-ink mb-space-4">学习目标</h4>
                    <ul class="space-y-space-2 text-ui text-slate">
                      {project.objectives.slice(0, 3).map((obj) => (
                        <li class="flex gap-space-3">
                          <span class="text-sky">•</span>
                          <span>{obj}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                  <div class="text-center md-card p-space-4 bg-softBlue">
                    <div class="text-ui mb-space-2 text-slate">所属周次</div>
                    <div class="font-bold text-h2 text-sky">
                      第 {project.weekNumber} 周
                    </div>
                  </div>
                </div>
              </div>
            </ScrollReveal>
          ))
        }
      </div>
    </div>
  </section>

  <section class="md-section md-section--sky">
    <div class="md-container">
      <div
        class="grid grid-cols-1 mx-auto max-w-4xl md:grid-cols-3 gap-space-10"
      >
        <div class="text-center md-card p-space-8">
          <div class="font-bold text-h2 mb-space-3 text-sky">5</div>
          <div class="text-body text-slate">个实战项目</div>
        </div>
        <div class="text-center md-card p-space-8">
          <div class="font-bold text-h2 mb-space-3 text-sky">44+</div>
          <div class="text-body text-slate">小时实践</div>
        </div>
        <div class="text-center md-card p-space-8">
          <div class="font-bold text-h2 mb-space-3 text-sky">10+</div>
          <div class="text-body text-slate">项技术栈</div>
        </div>
      </div>
    </div>
  </section>

  <section class="md-section">
    <div class="text-center md-container">
      <h2 class="font-bold text-h2 text-ink mb-space-8">
        准备好动手实践了吗？
      </h2>
      <p class="mx-auto max-w-2xl text-h3 mb-space-8 text-body text-slate">
        从第一个项目开始，体验 AI 辅助编程的强大力量
      </p>
      <a href={getUrl("projects/project-1")} class="md-btn"> 开始第一个项目 </a>
    </div>
  </section>
</PageLayout>
